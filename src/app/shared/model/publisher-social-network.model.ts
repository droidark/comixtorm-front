import {Publisher} from './publisher.model';

export class PublisherSocialNetwork {
  id: number;
  socialNetworkName: string;
  socialNetworkNickname: string;
  socialNetworkUrl: string;
  publisher: Publisher;
}
